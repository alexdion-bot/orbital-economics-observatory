# Pinned dependencies for the Orbital Economics Observatory pipeline.
#
# These are the versions the published results were produced with.
# Regenerate the exact set from your own environment with:
#     pip freeze > requirements.txt
# and replace this file, keeping only the packages you actually import.

pandas==2.2.3
numpy==2.1.3
python-docx==1.1.2
